# The Connection Club™ — Premium Members Portal

Private members-only portal for Connection Club™ subscribers.

## Structure
- `index.html` — Main portal page (password-protected)
- `css/style.css` — Portal styles
- `js/app.js` — Portal JavaScript
- `CNAME` — Custom domain config

## Preview
Push to `preview` branch → preview at the Vercel/Netlify preview URL.
Push to `main` branch → live at `club.tryconnecting.com`.
